# CS625 仿真与 NBV 工作日志 — 2026-07-24

## 今日目标

- 按 `HANDOFF_2026-07-21.md` 从 Step 1 继续恢复仿真管线。
- 在无法接触实机、深度相机和夹爪 IO 的远程条件下，优先验通 ROS 2、Gazebo、MoveIt、TF 与 NBV 软件链路。
- 保留全部实机接口和 RGB-D 场景配置，供回校后演示。

## 完成内容

1. 同步并编译 `cs625_state_monitor`，确认 `sim_state_publisher` 已安装。
2. 将 Gazebo 控制插件从旧版 `ign_ros2_control` 迁移到 ROS 2 Jazzy 的 `gz_ros2_control`。
3. 将控制器 YAML 迁移为 Jazzy `controller_manager.ros__parameters` 格式。
4. 修复 Gazebo 世界插件、模型资源路径、数据目录和启动参数。
5. 修复 MoveIt 仿真启动入口，显式启动使用 `moveit_2k.rviz` 的 RViz。
6. 增加 Gazebo → ROS `/clock` 单向桥接，使仿真时间、关节状态、TF、MoveIt 和状态监控使用同一时钟。
7. 增加 `enable_rgbd_sensor` 开关：
   - 默认 `false`：VMware 稳定控制模式，运行时移除 Sensors system。
   - 设为 `true`：保留完整 RGB-D 世界配置，供具备图形加速的环境继续验证。
8. 在 NBV 服务入口增加空点云保护，禁止无测量数据时生成 ICP 假收敛结果。

## 环境变更

- 安装 `ros-jazzy-gz-ros2-control`
- 安装 `ros-jazzy-moveit-ros-perception`
- 安装 `ros-jazzy-ros-gz-bridge`
- 完成 Ubuntu/ROS 软件包升级并重启

## 验证结果

### 构建

- `eli_cs_robot_simulation_gz`：通过
- `cs625_state_monitor`：通过
- `cs625_nbv`：通过

### 仿真控制与显示

- `joint_state_broadcaster`：`active`
- `joint_trajectory_controller`：`active`
- `/joint_states`：六轴数据正常，时间戳非零
- `/clock`：正常递增
- MoveIt `/move_group`：运行
- RViz：使用 2K 紧凑配置运行
- Gazebo 场景：地面、桌面、四条桌腿、目标物、遮挡墙、侧遮挡物和 CS625 模型均已加载

### TF 与状态监控

- `base_link -> my_end_effector_link`：可解析
- `world -> camera_link`：可解析
- `/cs625/raw_state`：
  - `parser_ok: true`
  - `has_joint_data: true`
  - `has_actual_tool_data: true`
  - `has_target_tool_data: true`

### NBV `fixed_order` 冒烟测试

- 服务 `/cs625_nbv/run_episode`：可发现、可调用
- NBV 服务端与候选视点发布节点：正常启动
- 首次测试发现空点云仍被报告为收敛；日志显示 PCL/ICP 输入为空，因此结果无效。
- 加入保护后复测结果：

```text
success=False
message=No point cloud received on /camera/points; episode not started
converged=False
total_views=0
```

这说明服务链路正常，并且不会再将缺少视觉输入的运行误记为有效实验。

## 已知限制

- VMware 当前没有可用的 3D 加速；启用 Gazebo Sensors system 后仿真循环会停滞。因此默认关闭 RGB-D Sensors system。
- 稳定模式下没有 `/camera/points`，暂时不能进行有意义的 ICP、点云融合、位姿协方差或 PoseGain-NBV 指标验证。
- 当前 NBV 的 MoveIt 规划/执行、IK 可达性和融合回调仍包含占位实现；今天验证的是系统冒烟链路，不是最终算法性能。
- `environment_point_cloud_publisher` 在没有 PCD 输入时会提示 merged cloud 为空，符合当前远程环境。

## 实机内容保留

- 未连接或控制真实 CS625。
- 未启动真实深度相机。
- 未操作夹爪数字 IO。
- 真实驱动、相机配置、夹爪 IO 与完整 RGB-D 世界配置均未删除，回校后可继续接入。

## 后续建议

1. 增加不依赖 GPU 的合成点云发布器，支持远程阶段验证 NBV 数据闭环。
2. 将 NBV 的规划占位回调替换为 MoveIt 规划服务，并接入 IK 可达性筛选。
3. 在具备图形加速的环境测试 `enable_rgbd_sensor:=true`。
4. 回校后依次完成真实相机驱动、手眼标定、夹爪 IO 和实机安全确认。

## 每日收尾约定

每次代码工作结束后生成当日工作日志，核对改动范围，提交并推送到用户 GitHub 仓库；不得把无关继承改动或实验生成物混入提交。
