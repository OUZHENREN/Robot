# 真机 NBV 存档：P4 完整纯虚拟批次

- 日期：2026-07-29
- 存档分支：`experiment/real-nbv`
- 主线来源提交：`4424487eea7dc9fa7283ae2c6fe81e18f6798957`（`main`）

## 已归档内容

本分支已合并 P4 批处理兼容性修正、LF 行尾保护，以及完整三遮挡虚拟批次的审计记录：

- 批处理接受 ROS 2 CLI 的 `success=True` 与 `success: true` 成功格式。
- manifest 明确写入 P4 的虚拟顺序信息融合与可观测性模型标识。
- P4 批次审计记录保留 90 个 episode、三场景、三策略、每单元 10 个匹配种子的执行与 MATLAB 结果。

## 结果状态与真机边界

完整虚拟数据满足预注册样本设计，但统计判定为失败：总体预测不确定性下降与下一步虚拟平移误差下降的相关为 `r=0.0401`，95% bootstrap CI 为 `[-0.0188, 0.1153]`；重遮挡为负相关。

该结果是**虚拟负结果**。本分支保留代码与日志以支持将来的实验室测试准备、可追溯性和对照，不授予真机执行权限，也不能作为真实相机、真实机械臂、CAD ADD/ADD-S 或 PoseGain 有效性的证据。任何实际硬件测试须独立完成安全审查、标定和新的实验设计。
